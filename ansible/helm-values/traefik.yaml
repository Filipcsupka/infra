deployment:
  replicas: 1

service:
  enabled: false

ingressClass:
  enabled: true
  isDefaultClass: true

ports:
  web:
    hostPort: 80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    hostPort: 443
