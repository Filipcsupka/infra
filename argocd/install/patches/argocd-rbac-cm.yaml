apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  # Default policy for authenticated users not matched by any role
  policy.default: role:readonly

  # Policies: p, <subject>, <resource>, <action>, <object>, allow|deny
  # g, <user/group>, <role>
  policy.csv: |
    # Admin role — full access
    p, role:admin, applications, *, */*, allow
    p, role:admin, clusters,     *, *,   allow
    p, role:admin, repositories, *, *,   allow
    p, role:admin, projects,     *, *,   allow
    p, role:admin, logs,         get, */*, allow
    p, role:admin, exec,         create, */*, allow

    # Read-only role (default)
    p, role:readonly, applications, get, */*, allow
    p, role:readonly, clusters,     get, *,   allow
    p, role:readonly, repositories, get, *,   allow

    # Assign admin role to your GitHub user (update username)
    g, Filipcsupka, role:admin
