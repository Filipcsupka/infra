apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GPU/AI workloads (ai-chat, k8s-ai-agent, observability, lobbyai) moved out
# entirely to the ai-infra repo on 2026-08-30 — k3sgpu is no longer a member
# of this cluster. See ai-infra repo README + ops-brain khtz.md. The only
# thing left here that talks to that box is the ai-chat-proxy routing shim
# under web/ (Service+EndpointSlice+Ingress, no scheduled pod).
resources:
  - core
  - web
