apiVersion: v1
kind: Service
metadata:
  name: app-2
  namespace: app-2
  labels:
    app: app-2
spec:
  type: ClusterIP
  selector:
    app: app-2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
