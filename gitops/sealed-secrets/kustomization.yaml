apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Populated by scripts/seal-monitoring-secrets.sh after sealed-secrets controller is running.
# Run the script, then commit the generated SealedSecret files alongside this kustomization.
resources: []
